# :80 serves the EC2 public IP over HTTP. Set CADDY_SITE_ADDRESS to a DNS name
# after its A record points here; Caddy then obtains and renews HTTPS itself.
{$CADDY_SITE_ADDRESS} {
    encode zstd gzip
    reverse_proxy api:8000
}
